services:
  consentledger:
    build: .
    image: cognis/consentledger:local
    restart: unless-stopped
    ports: ["8000:8000"]
    environment:
      - COGNIS_LOG_LEVEL=info
    volumes: ["./data:/data"]
